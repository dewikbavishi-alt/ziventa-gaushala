{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "framework": "nextjs",
  "regions": ["bom1"],

  "//": "Migrations are TEMPORARILY out of the build. The build command was `prisma migrate deploy && next build`, and builds stopped producing any deployment at all - consistent with migrate failing and `&&` halting the build. DIRECT_URL must be the Supabase SESSION POOLER (aws-0-ap-south-1.pooler.supabase.com:5432); the db.<ref>.supabase.co host has no IPv4 address and Vercel is IPv4-only, so it can never connect. Once DIRECT_URL is confirmed on Production, restore the migrate step so schema changes ship with the code that needs them. Until then, run `npm run db:deploy` locally after any schema change.",

  "buildCommand": "next build"
}
